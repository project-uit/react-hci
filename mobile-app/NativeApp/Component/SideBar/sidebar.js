import React, { Component } from 'react';
import { Container, Header, Left, Body, Right, Button, Title,Icon, Content, Text } from 'native-base';

type Props = {};

export default class SideBar extends Component<Props> {

    render() {

        return (
          <Content style={{backgroundColor: 'white'}}>
              <Text>con cặc</Text>
          </Content>
        );

    }
}